# No environment variables required for basic setup
# All data is statically configured in lib/data.ts

# Optional: For email integration (if you want to add backend contact form handling)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
